<?php
if (!empty($vote)) {
	switch ($vote['type']) {
		case 'plus':
		    include $this->template('plus.phtml', 'front', 'vote');
			break;

		case 'star':
            include $this->template('star.phtml', 'front', 'vote');
			break;	
	}
} 
?>